import { Bike, CheckCircle2, LocateFixed, MapPin, Navigation, Play, Radio, SkipForward } from "lucide-react";
import { useCallback, useEffect, useRef, useState } from "react";
import { io, type Socket } from "socket.io-client";
import { Header } from "../../components/Header";
import { useAuth } from "../../contexts/AuthContext";
import { api, SOCKET_URL } from "../../lib/api";
interface Stop{id:string;sequence:number;status:string;estimatedDistanceKm?:string;estimatedDurationMin?:number;order:{reference:string;customerName:string;customerPhone:string;deliveryAddress?:string;deliveryLatitude?:string;deliveryLongitude?:string}}
interface Assignment{id:string;status:string;stops:Stop[]}
export function DriverPage(){const{token,user}=useAuth();const[items,setItems]=useState<Assignment[]>([]);const[sharing,setSharing]=useState<string>();const[error,setError]=useState("");const socket=useRef<Socket | undefined>(undefined);const watch=useRef<number | undefined>(undefined);const lastSent=useRef(0);const load=useCallback(()=>api<Assignment[]>("/driver/delivery/assignments",{},token).then(setItems),[token]);useEffect(()=>{void load();return()=>{if(watch.current!==undefined)navigator.geolocation.clearWatch(watch.current);socket.current?.disconnect();}},[load]);const start=async(id:string)=>{try{await api(`/driver/delivery/assignments/${id}/start`,{method:"PATCH"},token);startSharing(id);load()}catch(e){setError(e instanceof Error?e.message:"Démarrage impossible")}};const startSharing=(assignmentId:string)=>{if(!navigator.geolocation){setError("La géolocalisation n’est pas disponible.");return}socket.current=io(`${SOCKET_URL}/delivery`,{auth:{token},transports:["websocket"]});watch.current=navigator.geolocation.watchPosition(({coords})=>{if(Date.now()-lastSent.current<5000)return;lastSent.current=Date.now();socket.current?.emit("driver:location",{assignmentId,latitude:coords.latitude,longitude:coords.longitude,accuracy:coords.accuracy,heading:coords.heading,speed:coords.speed})},()=>setError("Autorisez la position GPS pour démarrer la tournée."),{enableHighAccuracy:true,maximumAge:3000,timeout:15000});setSharing(assignmentId)};const stop=async(id:string,status:"ARRIVED"|"DELIVERED"|"SKIPPED")=>{await api(`/driver/delivery/stops/${id}`,{method:"PATCH",body:JSON.stringify({status})},token);load()};return <><Header/><main className="page container driver-page"><div className="driver-welcome"><span className="eyebrow">Bonjour {user?.displayName}</span><h1>Mes livraisons</h1><p>Gardez cet écran ouvert pendant votre tournée.</p></div>{error&&<div className="error-banner">{error}</div>}{items.map(a=><section className="driver-assignment" key={a.id}><header><div><span className={sharing===a.id?"gps-pulse active":"gps-pulse"}><Radio/></span><div><small>Tournée</small><strong>{a.stops.length} livraison(s)</strong></div></div><span className="order-badge confirmed">{a.status}</span></header>{a.status==="PLANNED"?<button className="button primary wide" onClick={()=>start(a.id)}><Play/> Démarrer et partager ma position</button>:sharing!==a.id?<button className="button primary wide" onClick={()=>startSharing(a.id)}><LocateFixed/> Reprendre le partage GPS</button>:<div className="sharing-banner"><Navigation/> Position partagée en direct</div>}<div className="driver-stops">{a.stops.map(s=><article className={s.status==="DELIVERED"?"delivered":""} key={s.id}><div className="stop-index">{s.status==="DELIVERED"?<CheckCircle2/>:s.sequence}</div><div className="stop-content"><span>{s.order.reference}</span><h3>{s.order.customerName}</h3><p><MapPin/>{s.order.deliveryAddress||"Coordonnées GPS disponibles"}</p><p>{s.estimatedDistanceKm&&`${s.estimatedDistanceKm} km · `}{s.estimatedDurationMin&&`environ ${s.estimatedDurationMin} min`}</p>{s.status!=="DELIVERED"&&<div className="stop-actions"><a className="button subtle" href={`https://www.google.com/maps/dir/?api=1&destination=${s.order.deliveryLatitude},${s.order.deliveryLongitude}`} target="_blank"><Navigation/> Itinéraire</a>{s.status==="PENDING"&&<button className="button subtle" onClick={()=>stop(s.id,"ARRIVED")}><MapPin/> Je suis arrivé</button>}<button className="button primary" onClick={()=>stop(s.id,"DELIVERED")}><CheckCircle2/> Livré</button><button className="icon-button" onClick={()=>stop(s.id,"SKIPPED")} title="Passer"><SkipForward/></button></div>}</div></article>)}</div></section>)}{!items.length&&<div className="empty-state large"><Bike/><h2>Aucune livraison assignée</h2><p>Les nouvelles tournées apparaîtront ici.</p></div>}</main></>}
